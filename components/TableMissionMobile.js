// components/TableMissionMobile.js
import React from "react";

export default function TableMissionMobile({ mission }) {
  return (
    <div className="flex flex-wrap items-center whitespace-normal py-1">
      <p>{mission}</p>
    </div>
  );
}
